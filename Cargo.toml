[package]
name = "lb"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = " 1.36.0", features = [
  "net",
  "rt-multi-thread",
  "macros",
  "full",
] }
anyhow = "1.0.80"
